# Data Directory

No data are stored in this repository.

Place restricted data outside the repository and point `configs/paths.yaml` to those local files.

The scripts refer to data through local configuration only.
